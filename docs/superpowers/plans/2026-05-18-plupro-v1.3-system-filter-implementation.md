# PluPro v1.3 — HIC/EVS 系统筛选 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 v1.1 已埋好但未消费的 `system: HIC | EVS` frontmatter 字段在视图层用起来:控制塔加 segmented control 4 选 1 筛选 + 「全部」视图按 system 分组渲染。

**Architecture:** types.ts 加 ProjectSystem 枚举,ProjectManifestScanner 加字段解析,ControlTowerView 加 filter 状态 + 分组渲染函数 + segmented control UI。无新核心模块,纯视图层增强。

**Tech Stack:** TypeScript 5.x / Obsidian 1.4+ / esbuild / Jest 29.x / gray-matter

**Spec:** [docs/superpowers/specs/2026-05-18-plupro-v1.3-system-filter-design.md](../specs/2026-05-18-plupro-v1.3-system-filter-design.md)

---

## Task 1: 类型扩展 + scanner 字段解析(TDD)

**Files:**
- Modify: `src/types.ts`
- Modify: `src/core/ProjectManifestScanner.ts`
- Modify: `tests/ProjectManifestScanner.spec.ts`

### Step 1: 写失败测试

追加到 `tests/ProjectManifestScanner.spec.ts` 最后一个 it 之后(`});` 之前):

```typescript
  it('提取 system: HIC 字段', () => {
    const text = `---
type: project
slug: demo-proj
title: Demo
status: active
system: HIC
---
`;
    const m = parseManifestFromText(text, '_projects/demo-proj.md')!;
    expect(m.system).toBe('HIC');
  });

  it('提取 system: EVS 字段', () => {
    const text = `---
type: project
slug: demo-proj
title: Demo
status: active
system: EVS
---
`;
    const m = parseManifestFromText(text, '_projects/demo-proj.md')!;
    expect(m.system).toBe('EVS');
  });

  it('非法 system 值回退为 undefined', () => {
    const text = `---
type: project
slug: demo-proj
title: Demo
status: active
system: legacy
---
`;
    const m = parseManifestFromText(text, '_projects/demo-proj.md')!;
    expect(m.system).toBeUndefined();
  });

  it('缺 system 字段为 undefined', () => {
    const text = `---
type: project
slug: demo-proj
title: Demo
status: active
---
`;
    const m = parseManifestFromText(text, '_projects/demo-proj.md')!;
    expect(m.system).toBeUndefined();
  });
```

### Step 2: 跑测试验证失败

Run: `npx jest ProjectManifestScanner`
Expected: 4 个新测试 FAIL(`m.system` undefined 即使输入是 HIC),原 6 个 PASS

### Step 3: 扩展 ProjectManifest 类型

修改 `src/types.ts`,在 `ProjectStatus` type alias 之后追加:

```typescript
export type ProjectSystem = 'HIC' | 'EVS';

export type SystemFilter = 'all' | 'HIC' | 'EVS' | 'unassigned';
```

然后在 `ProjectManifest` interface 内 `lastAnalyzed?: string;` 之后追加 1 行:

```typescript
  system?: ProjectSystem;
```

完整的 `ProjectManifest` 应该是:

```typescript
export interface ProjectManifest {
  slug: ProjectSlug;
  title: string;
  status: ProjectStatus;
  owner?: string;
  created?: string;
  updated?: string;
  scope?: string[];
  tags?: string[];
  manifestPath: string;
  pendingAnalysis?: boolean;
  generatedChanges?: string[];
  lastAnalyzed?: string;
  system?: ProjectSystem;
}
```

### Step 4: scanner 加字段解析

修改 `src/core/ProjectManifestScanner.ts`:

第一步:在 `VALID_STATUSES` 常量之后追加:

```typescript
const VALID_SYSTEMS: ProjectSystem[] = ['HIC', 'EVS'];
```

第二步:import 加上 `ProjectSystem`(`import type { ProjectManifest, ProjectStatus, ProjectSystem } from '../types';`)

第三步:在 `parseManifestFromText` 的 return 块,`lastAnalyzed: asString(fm['last-analyzed']),` 之后追加:

```typescript
    system: (() => {
      const raw = asString(fm.system);
      return raw && VALID_SYSTEMS.includes(raw as ProjectSystem)
        ? (raw as ProjectSystem)
        : undefined;
    })(),
```

### Step 5: 跑测试验证全部通过

Run: `npx jest`
Expected: 全量 51 tests PASS(原 47 + 新 4)

### Step 6: typecheck

Run: `npm run typecheck`
Expected: 无错误

### Step 7: 暂存(不 commit)

```bash
git add src/types.ts src/core/ProjectManifestScanner.ts tests/ProjectManifestScanner.spec.ts
git status
```

---

## Task 2: ControlTowerView 加 systemFilter 状态 + 计数 helper

**Files:**
- Modify: `src/view/ControlTowerView.ts`

### Step 1: import 加 SystemFilter 类型

修改 `src/view/ControlTowerView.ts` 第 2 行 import:

```typescript
import type { ChangeEntry, ProjectEntry, ProjectIndexSnapshot, SystemFilter } from '../types';
```

### Step 2: 加私有字段

在 `private selectedProjectSlug: string | null = null;` 之后追加:

```typescript
  private systemFilter: SystemFilter = 'all';
```

### Step 3: 加 countBySystem helper(类内 private 方法)

在 `private renderProgressBar(...)` 之前追加:

```typescript
  /**
   * 按 system 字段聚合 4 类计数,供 segmented control 显示。
   */
  private countBySystem(snapshot: ProjectIndexSnapshot): Record<SystemFilter, number> {
    const counts: Record<SystemFilter, number> = { all: 0, HIC: 0, EVS: 0, unassigned: 0 };
    for (const entry of snapshot.projects.values()) {
      counts.all += 1;
      const key = (entry.manifest.system ?? 'unassigned') as SystemFilter;
      counts[key] += 1;
    }
    return counts;
  }

  /**
   * 按 systemFilter 过滤项目,分组到 HIC / EVS / unassigned 三个桶,桶内按 title
   * 中文字典序排序,空桶被过滤掉。组间顺序固定 HIC → EVS → unassigned。
   */
  private groupProjectsBySystem(
    snapshot: ProjectIndexSnapshot,
  ): Array<{ system: SystemFilter; entries: ProjectEntry[] }> {
    const buckets = new Map<SystemFilter, ProjectEntry[]>();
    for (const entry of snapshot.projects.values()) {
      const key = (entry.manifest.system ?? 'unassigned') as SystemFilter;
      if (this.systemFilter !== 'all' && this.systemFilter !== key) continue;
      const arr = buckets.get(key) ?? [];
      arr.push(entry);
      buckets.set(key, arr);
    }
    const collator = new Intl.Collator('zh-CN');
    for (const arr of buckets.values()) {
      arr.sort((a, b) => collator.compare(a.manifest.title, b.manifest.title));
    }
    const order: SystemFilter[] = ['HIC', 'EVS', 'unassigned'];
    return order
      .map((system) => ({ system, entries: buckets.get(system) ?? [] }))
      .filter((g) => g.entries.length > 0);
  }
```

### Step 4: typecheck

Run: `npm run typecheck`
Expected: 无错误

### Step 5: jest 不退化

Run: `npx jest`
Expected: 51 tests PASS

### Step 6: 暂存

```bash
git add src/view/ControlTowerView.ts
git status
```

---

## Task 3: renderList 加 segmented control 渲染

**Files:**
- Modify: `src/view/ControlTowerView.ts`

### Step 1: 在 renderList 内插入 filter bar

定位 `renderList` 内 `const refreshBtn = header.createEl(...)` 之**后**(`refreshBtn.addEventListener` block 结束之后),追加:

```typescript
    // === v1.3 新增:segmented control 系统筛选 ===
    const counts = this.countBySystem(snapshot);
    const filterBar = this.listPaneEl.createDiv({ cls: 'plupro-filter-bar' });
    const filters: Array<{ key: SystemFilter; label: string; icon: string }> = [
      { key: 'all',        label: '全部',     icon: '' },
      { key: 'HIC',        label: 'HIC',     icon: '🟢' },
      { key: 'EVS',        label: 'EVS',     icon: '🟠' },
      { key: 'unassigned', label: '未分类',  icon: '⚪' },
    ];
    for (const f of filters) {
      const chip = filterBar.createEl('button', {
        cls: 'plupro-filter-chip' + (this.systemFilter === f.key ? ' is-active' : ''),
        text: `${f.icon} ${f.label} (${counts[f.key]})`.trim(),
      });
      chip.addEventListener('click', (evt) => {
        evt.stopPropagation();
        this.systemFilter = f.key;
        this.renderAll(this.plugin.getIndex());
      });
    }
```

### Step 2: typecheck

Run: `npm run typecheck`
Expected: 无错误

### Step 3: jest 不退化 + build

Run: `npx jest && npm run build`
Expected: 51 tests PASS,dist/main.js 重新生成

### Step 4: 暂存

```bash
git add src/view/ControlTowerView.ts
git status
```

---

## Task 4: renderList 项目循环改为按 system 分组渲染

**Files:**
- Modify: `src/view/ControlTowerView.ts`

### Step 1: 替换项目 ul 渲染逻辑

定位 `renderList` 内 `const ul = this.listPaneEl.createEl('ul', { cls: 'plupro-project-list' });` 这一行开始,直到 `for (const [slug, entry] of snapshot.projects)` 闭合 block 结束的整段 — 完整替换为:

```typescript
    const groups = this.groupProjectsBySystem(snapshot);

    if (groups.length === 0) {
      this.listPaneEl.createDiv({
        cls: 'plupro-empty',
        text: this.systemFilter === 'all'
          ? '尚无项目 — 在 _projects/ 下创建 manifest 文件即可'
          : `${this.systemFilter} 系统下没有项目`,
      });
    }

    const ul = this.listPaneEl.createEl('ul', { cls: 'plupro-project-list' });

    const systemLabels: Record<SystemFilter, { icon: string; text: string }> = {
      all:        { icon: '',    text: '' },
      HIC:        { icon: '🟢', text: 'HIC 系统' },
      EVS:        { icon: '🟠', text: 'EVS 系统' },
      unassigned: { icon: '⚪', text: '未分类' },
    };

    for (const group of groups) {
      // 只在「全部」视图下渲染 divider(单组视图无必要)
      if (this.systemFilter === 'all') {
        const label = systemLabels[group.system];
        ul.createEl('li', {
          cls: 'plupro-system-divider',
          text: `━━━ ${label.icon} ${label.text}(${group.entries.length})━━━`,
        });
      }

      for (const entry of group.entries) {
        const slug = entry.manifest.slug;
        const li = ul.createEl('li', { cls: 'plupro-project-item' });
        if (slug === this.selectedProjectSlug) {
          li.addClass('is-selected');
        }
        const title = li.createDiv({ cls: 'plupro-project-title', text: entry.manifest.title });
        title.setAttr('data-slug', slug);
        const fm = entry.manifest;
        if (fm.pendingAnalysis) {
          title.createSpan({ cls: 'plupro-badge plupro-badge-pending', text: ' 🟡 待分析' });
        } else if (fm.generatedChanges && fm.generatedChanges.length > 0) {
          title.createSpan({
            cls: 'plupro-badge plupro-badge-done',
            text: ` 🟢 已拆解(${fm.generatedChanges.length})`,
          });
        }
        const meta = li.createDiv({ cls: 'plupro-project-meta' });
        const pct = entry.progress.totalCount === 0
          ? 0
          : Math.round((entry.progress.totalDone / entry.progress.totalCount) * 100);
        meta.setText(
          `${entry.progress.changesDone}/${entry.progress.changeCount} change · ${entry.progress.totalDone}/${entry.progress.totalCount} task · ${pct}%`,
        );
        this.renderProgressBar(li, pct);
        li.addEventListener('click', () => {
          this.selectedProjectSlug = slug;
          this.renderAll(this.plugin.getIndex());
        });
      }
    }
```

注:这段替换保留了原 `slugConflicts banner` 渲染(因为它在循环之**前**)和原"📦 未分类(N) change 桶"渲染(在循环之**后**),只替换中间的项目 li 循环。

### Step 2: typecheck

Run: `npm run typecheck`
Expected: 无错误

### Step 3: jest 不退化 + build

Run: `npx jest && npm run build`
Expected: 51 tests PASS,dist/main.js 重新生成

### Step 4: 暂存

```bash
git add src/view/ControlTowerView.ts
git status
```

---

## Task 5: CSS — filter chip + system divider

**Files:**
- Modify: `styles.css`

### Step 1: 追加 CSS

修改 `styles.css`,在文件末尾追加:

```css
.plupro-filter-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
  margin-bottom: 12px;
}
.plupro-filter-chip {
  padding: 3px 10px;
  border-radius: 12px;
  cursor: pointer;
  font-size: 0.8em;
  background-color: var(--background-secondary);
  color: var(--text-normal);
  border: 1px solid var(--background-modifier-border);
}
.plupro-filter-chip:hover {
  background-color: var(--background-modifier-hover);
}
.plupro-filter-chip.is-active {
  background-color: var(--interactive-accent);
  color: var(--text-on-accent);
  border-color: var(--interactive-accent);
}
.plupro-system-divider {
  font-size: 0.75em;
  color: var(--text-muted);
  font-weight: 500;
  padding: 8px 0 4px 0;
  margin: 8px 0 4px 0;
  list-style: none;
  text-align: center;
}
```

### Step 2: typecheck + build

Run: `npm run typecheck && npm run build`
Expected: 无错误

### Step 3: 暂存

```bash
git add styles.css
git status
```

---

## Task 6: 版本号升级 + 冒烟 checklist 流程 6

**Files:**
- Modify: `manifest.json`
- Modify: `package.json`
- Modify: `docs/smoke-test.md`

### Step 1: 升级 manifest.json

修改 `manifest.json` 的 `"version"`:

```json
"version": "0.3.0",
```

### Step 2: 升级 package.json

修改 `package.json` 的 `"version"`:

```json
"version": "0.3.0",
```

### Step 3: 更新冒烟 checklist 加流程 6

修改 `docs/smoke-test.md`,在 `## 已知 v1 限制(非缺陷)` **之前**插入:

```markdown
## 流程 6:HIC/EVS 系统筛选与分组(v1.3 新增)

- [ ] 6.1 打开控制塔,列表头部「项目 / ⟳ 刷新」之下出现 4 个 chip:全部 / HIC / EVS / 未分类
- [ ] 6.2 默认 chip 激活态为「全部」,蓝色背景
- [ ] 6.3 chip 计数:全部 = HIC + EVS + 未分类,各自数值匹配 _projects/ 实际 manifest 数
- [ ] 6.4 「全部」视图下出现 3 个 divider:━━━ 🟢 HIC 系统(N)━━━ 等
- [ ] 6.5 点 HIC chip:只显示 HIC 系统下项目,无 divider,EVS chip 计数不变
- [ ] 6.6 点 EVS chip:只显示 EVS 系统下项目
- [ ] 6.7 点未分类 chip:显示「自动生成标准层叠编码」「自动设置规则」等无 system 字段的项目
- [ ] 6.8 切换 chip 时,右侧已选中项目卡片状态不消失

```

### Step 4: typecheck + jest + build

Run: `npm run typecheck && npx jest && npm run build`
Expected: 无错误,51 tests PASS,dist/main.js 重新生成

### Step 5: 暂存

```bash
git add manifest.json package.json docs/smoke-test.md
git status
```

---

## Task 7: 整合 commit + 部署验证

**Files:**
- 所有前 6 task 的 staged 改动 + spec + plan

### Step 1: 确认 staged 内容

Run: `git diff --cached --stat`
Expected: 应当看到:
- `src/types.ts`
- `src/core/ProjectManifestScanner.ts`
- `src/view/ControlTowerView.ts`
- `styles.css`
- `manifest.json`
- `package.json`
- `tests/ProjectManifestScanner.spec.ts`
- `docs/smoke-test.md`

### Step 2: 把 spec + plan 文件也 add

```bash
git add docs/superpowers/specs/2026-05-18-plupro-v1.3-system-filter-design.md
git add docs/superpowers/plans/2026-05-18-plupro-v1.3-system-filter-implementation.md
```

### Step 3: Final commit

```bash
git commit -m "$(cat <<'EOF'
feat(v1.3): HIC/EVS 顶层系统筛选与分组显示

把 v1.1 已埋好但未消费的 system: HIC | EVS frontmatter 字段在视图
层用起来:控制塔列表头部加 segmented control(全部 / HIC / EVS /
未分类 4 chip),「全部」视图下按 system 分组渲染,组间 divider
分隔。

types.ts 加 ProjectSystem / SystemFilter 类型;ProjectManifestScanner
读取 system 字段(VALID_SYSTEMS 白名单校验,非法值静默回退 undefined);
ControlTowerView 加 systemFilter 状态 + countBySystem helper +
groupProjectsBySystem 排序函数;renderList 重写项目循环为分组渲染;
styles.css 加 .plupro-filter-bar / .plupro-filter-chip / .plupro-
system-divider 样式。

版本 0.2.0 → 0.3.0;冒烟 checklist 加流程 6(8 个验证子项);
51 tests 全过,typecheck + build OK。

spec:docs/superpowers/specs/2026-05-18-plupro-v1.3-system-filter-design.md
plan:docs/superpowers/plans/2026-05-18-plupro-v1.3-system-filter-implementation.md
EOF
)"
```

### Step 4: 验证 commit + 部署

```bash
git log --oneline -3
ls -la /Users/danwei/Documents/HIC/.obsidian/plugins/obsidian-plu-pro/main.js
```

Expected: 最新 commit `feat(v1.3): HIC/EVS 顶层系统筛选与分组显示` 存在,symlink 指向新 dist/main.js。

### Step 5: Obsidian 重载提示(用户操作)

- Obsidian → 设置 → 第三方插件 → "PluPro 项目控制塔" → 关闭再启用

### Step 6: 跑流程 6 冒烟(用户操作)

按 `docs/smoke-test.md` 的"流程 6"逐项验证。

---

## 完成定义(v1.3 Done)

- [ ] 7 个 task 全部 checkbox 打勾
- [ ] `npm test` 51/51 pass
- [ ] `npm run typecheck` 无错误
- [ ] `npm run build` 产出 `dist/main.js`
- [ ] 冒烟流程 6 的 8 个子项手工通过
- [ ] manifest.json + package.json 版本均为 `0.3.0`
